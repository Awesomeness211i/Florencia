#![allow(bad_style)]

use FloEditor::FloEditor;
use FlorenciaEngine::*;

use anyhow::Result;

fn main() -> Result<()> {
	//let mut app = FloEditor::new()?;
	//return app.Run();
	FlorenciaEngine::test()
}
